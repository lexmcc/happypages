<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Specs - happypages" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white">
    <div class="min-h-screen flex flex-col">
      <header class="flex-none border-b border-gray-100 px-4 py-3">
        <div class="max-w-3xl mx-auto flex items-center gap-2">
          <svg class="size-5 text-[#ff584d]" viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="12" r="10"/></svg>
          <span class="text-sm font-semibold text-gray-900">happypages</span>
        </div>
      </header>
      <main class="flex-1 flex flex-col">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
