<!DOCTYPE html>
<html>
  <head>
    <title>Super Admin Login</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-ops-bg min-h-screen flex items-center justify-center">
    <div class="w-full max-w-sm mx-auto px-4">
      <% if flash[:alert] %>
        <div class="mb-4 px-4 py-3 bg-ops-red/10 border border-ops-red/20 rounded-lg text-ops-red text-sm">
          <%= flash[:alert] %>
        </div>
      <% end %>
      <% if flash[:notice] %>
        <div class="mb-4 px-4 py-3 bg-ops-green/10 border border-ops-green/20 rounded-lg text-ops-green text-sm">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
