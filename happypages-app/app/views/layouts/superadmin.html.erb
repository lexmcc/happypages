<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Super Admin - Happypages" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" href="/icon.png" type="image/png">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-50">
    <div class="flex min-h-screen">
      <%= render "superadmin/shared/sidebar" %>
      <main class="flex-1 min-w-0 lg:ml-0">
        <% if flash[:alert] %>
          <div class="mx-4 mt-4 px-4 py-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm">
            <%= flash[:alert] %>
          </div>
        <% end %>
        <% if flash[:notice] %>
          <div class="mx-4 mt-4 px-4 py-3 bg-green-50 border border-green-200 rounded-lg text-green-700 text-sm">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
