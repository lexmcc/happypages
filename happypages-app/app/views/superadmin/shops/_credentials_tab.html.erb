<div class="bg-ops-surface rounded-xl border border-ops-border-strong shadow-sm overflow-hidden">
  <table class="min-w-full">
    <tbody class="divide-y divide-ops-border">
      <tr>
        <td class="px-6 py-4 text-sm font-medium text-ops-text-secondary w-48">Platform</td>
        <td class="px-6 py-4 text-sm text-ops-text"><%= @shop.platform_type.capitalize %></td>
      </tr>
      <tr>
        <td class="px-6 py-4 text-sm font-medium text-ops-text-secondary">Access Token</td>
        <td class="px-6 py-4">
          <% if @credential&.shopify_access_token.present? %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-ops-green/10 text-ops-green">Present</span>
          <% else %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-ops-red/10 text-ops-red">Missing</span>
          <% end %>
        </td>
      </tr>
      <tr>
        <td class="px-6 py-4 text-sm font-medium text-ops-text-secondary">Awtomic</td>
        <td class="px-6 py-4">
          <% if @credential&.awtomic_api_key.present? %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-ops-green/10 text-ops-green">Connected</span>
          <% else %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-ops-surface-hover text-ops-text-tertiary">Not connected</span>
          <% end %>
        </td>
      </tr>
      <tr>
        <td class="px-6 py-4 text-sm font-medium text-ops-text-secondary">Klaviyo</td>
        <td class="px-6 py-4">
          <% if @credential&.klaviyo_api_key.present? %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-ops-green/10 text-ops-green">Connected</span>
          <% else %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-ops-surface-hover text-ops-text-tertiary">Not connected</span>
          <% end %>
        </td>
      </tr>
      <tr>
        <td class="px-6 py-4 text-sm font-medium text-ops-text-secondary">Shop Slug</td>
        <td class="px-6 py-4 text-sm text-ops-text font-mono"><%= @shop.slug || "—" %></td>
      </tr>
      <tr>
        <td class="px-6 py-4 text-sm font-medium text-ops-text-secondary">Storefront URL</td>
        <td class="px-6 py-4 text-sm text-ops-text"><%= @shop.customer_facing_url %></td>
      </tr>
    </tbody>
  </table>
</div>
