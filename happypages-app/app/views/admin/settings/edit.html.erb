<div class="min-h-screen bg-[#f9f9f9] py-8">
  <div class="max-w-3xl mx-auto px-4 sm:px-6">
    <header class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900">Settings</h1>
      <p class="text-gray-600 mt-2">Manage your shop configuration</p>
    </header>

    <% if notice %>
      <div class="mb-6 p-4 bg-emerald-50 border border-emerald-200 rounded-lg text-emerald-700 text-sm">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm">
        <%= alert %>
      </div>
    <% end %>

    <% if @shop %>
      <section>
        <div class="bg-[#f4f4f0] rounded-xl border border-black/5 shadow-[inset_1px_1px_0_rgba(255,255,255,1),inset_-1px_-1px_0_rgba(0,0,0,0.05),0_4px_8px_rgba(0,0,0,0.05)] overflow-hidden">
          <div class="px-6 py-4 border-b border-black/5">
            <h3 class="text-lg font-semibold text-gray-900">White-Labeled Referral URL</h3>
            <p class="text-sm text-gray-500">Customize your shop's referral page URL slug</p>
          </div>

          <div class="p-6">
            <%= form_with url: admin_settings_path, method: :patch, local: true, class: "space-y-4" do %>
              <div class="max-w-md">
                <label for="shop_slug" class="block text-sm font-medium text-gray-700 mb-1">
                  URL Slug
                </label>
                <div class="flex items-center gap-2">
                  <span class="text-gray-500 text-sm">https://app.happypages.co/</span>
                  <input
                    type="text"
                    name="shop_slug"
                    id="shop_slug"
                    value="<%= @shop.slug %>"
                    pattern="[a-z0-9-]+"
                    minlength="3"
                    maxlength="50"
                    class="flex-1 px-4 py-2 bg-[#f9f9f9] border border-black/10 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#ff584d] focus:border-[#ff584d]"
                    placeholder="your-shop-name"
                  >
                  <span class="text-gray-500 text-sm">/refer</span>
                </div>
                <p class="mt-1 text-xs text-gray-500">Only lowercase letters, numbers, and hyphens. 3-50 characters.</p>
                <% if @shop.slug.present? %>
                  <p class="mt-2 text-sm text-gray-600">
                    Current referral URL:
                    <a href="https://app.happypages.co/<%= @shop.slug %>/refer" target="_blank" class="text-[#ff584d] hover:underline">
                      https://app.happypages.co/<%= @shop.slug %>/refer
                    </a>
                  </p>
                <% end %>
              </div>

              <div>
                <button type="submit" class="px-4 py-2 bg-[#ff584d] text-white rounded-lg hover:bg-[#e64d43] transition-colors font-medium text-sm">
                  Save Settings
                </button>
              </div>
            <% end %>
          </div>
        </div>
      </section>

    <% if @shop.slug.present? %>
      <section class="mt-6" data-controller="tabs">
        <div class="bg-[#f4f4f0] rounded-xl border border-black/5 shadow-[inset_1px_1px_0_rgba(255,255,255,1),inset_-1px_-1px_0_rgba(0,0,0,0.05),0_4px_8px_rgba(0,0,0,0.05)] overflow-hidden">
          <div class="px-6 py-4 border-b border-black/5">
            <h3 class="text-lg font-semibold text-gray-900">Theme Integration</h3>
            <p class="text-sm text-gray-500">Add referral support to your storefront</p>

            <nav class="flex gap-4 mt-3" role="tablist">
              <button
                type="button"
                data-tabs-target="tab"
                data-action="click->tabs#switch"
                data-index="0"
                role="tab"
                class="pb-2 text-sm font-medium border-b-2 transition-colors border-slate-900 text-slate-900"
              >
                Shopify Theme
              </button>
              <button
                type="button"
                data-tabs-target="tab"
                data-action="click->tabs#switch"
                data-index="1"
                role="tab"
                class="pb-2 text-sm font-medium border-b-2 transition-colors border-transparent text-gray-500 hover:text-gray-700"
              >
                Hydrogen
              </button>
            </nav>
          </div>

          <%# Shopify Theme tab %>
          <div data-tabs-target="panel" class="p-6 space-y-4">
            <p class="text-sm text-gray-700">Paste this Liquid snippet into your theme (e.g. header, account page, or navigation) to show a "My Referrals" link to logged-in customers:</p>

            <div class="relative">
              <pre class="bg-gray-900 text-gray-100 rounded-lg p-4 text-sm overflow-x-auto"><code>{% raw %}{%- assign slug = shop.metafields['app--happypages-friendly-referrals'].shop_slug -%}
{%- assign code = customer.metafields['app--happypages-friendly-referrals'].referral_code -%}
{%- if slug and code -%}
  <a href="https://app.happypages.co/{{ slug }}/refer?code={{ code }}">
    My Referrals
  </a>
{%- endif -%}{% endraw %}</code></pre>
              <button
                type="button"
                onclick="navigator.clipboard.writeText(this.closest('.relative').querySelector('code').textContent).then(() => { this.textContent = 'Copied!'; setTimeout(() => { this.textContent = 'Copy'; }, 2000); })"
                class="absolute top-2 right-2 px-2.5 py-1 bg-gray-700 hover:bg-gray-600 text-gray-200 text-xs rounded transition-colors"
              >
                Copy
              </button>
            </div>

            <p class="text-xs text-gray-500 border-t border-black/5 pt-3">
              The link only appears when the customer is logged in and has a referral code. The URL updates automatically if you change your slug above.
            </p>
          </div>

          <%# Hydrogen tab %>
          <div data-tabs-target="panel" class="hidden p-6 space-y-6">
            <%= form_with url: admin_settings_path, method: :patch, local: true, class: "space-y-6" do %>
              <div class="max-w-md">
                <label for="storefront_url" class="block text-sm font-medium text-gray-700 mb-1">
                  Storefront URL
                </label>
                <input
                  type="url"
                  name="storefront_url"
                  id="storefront_url"
                  value="<%= @shop.storefront_url %>"
                  class="w-full px-4 py-2 bg-[#f9f9f9] border border-black/10 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#ff584d] focus:border-[#ff584d]"
                  placeholder="https://your-store.com"
                >
                <p class="mt-1 text-xs text-gray-500">Your Hydrogen storefront URL — used in share links so customers land on your headless store.</p>
              </div>

              <div>
                <button type="submit" class="px-4 py-2 bg-[#ff584d] text-white rounded-lg hover:bg-[#e64d43] transition-colors font-medium text-sm">
                  Save Storefront URL
                </button>
              </div>
            <% end %>

            <div>
              <p class="text-sm text-gray-700 mb-3">Drop this file into your Hydrogen app at <code class="bg-gray-100 px-1.5 py-0.5 rounded text-xs">app/routes/discount.$code.tsx</code> to enable the <code class="bg-gray-100 px-1.5 py-0.5 rounded text-xs">/discount/CODE</code> route:</p>

              <div class="relative">
                <pre class="bg-gray-900 text-gray-100 rounded-lg p-4 text-sm overflow-x-auto leading-relaxed"><code>import { redirect, type LoaderFunctionArgs } from '@shopify/remix-oxygen';

export async function loader({ params, context }: LoaderFunctionArgs) {
  const code = params.code;
  if (!code) throw new Response('Discount code required', { status: 400 });

  const { storefront } = context;

  const { cart } = await storefront.mutate(CREATE_CART_WITH_DISCOUNT, {
    variables: { discountCodes: [code] },
  });

  const headers = cart?.id
    ? new Headers({ 'Set-Cookie': `cart=${cart.id}; Path=/; HttpOnly; SameSite=Lax` })
    : new Headers();

  return redirect('/', { headers });
}

const CREATE_CART_WITH_DISCOUNT = `#graphql
  mutation CreateCartWithDiscount($discountCodes: [String!]) {
    cartCreate(input: { discountCodes: $discountCodes }) {
      cart {
        id
        discountCodes {
          code
          applicable
        }
      }
    }
  }
`;</code></pre>
                <button
                  type="button"
                  onclick="navigator.clipboard.writeText(this.closest('.relative').querySelector('code').textContent).then(() => { this.textContent = 'Copied!'; setTimeout(() => { this.textContent = 'Copy'; }, 2000); })"
                  class="absolute top-2 right-2 px-2.5 py-1 bg-gray-700 hover:bg-gray-600 text-gray-200 text-xs rounded transition-colors"
                >
                  Copy
                </button>
              </div>
            </div>

            <div class="text-xs text-gray-500 border-t border-black/5 pt-3 space-y-2">
              <p>This route creates a cart with the discount code applied and redirects to your homepage — matching the built-in <code class="bg-gray-100 px-1 rounded">/discount/CODE</code> behavior of standard Shopify themes.</p>
              <p>Uses Hydrogen's <code class="bg-gray-100 px-1 rounded">context.storefront</code> — no extra tokens needed. If your app uses a custom cart provider, you may need to adapt this to use your cart's <code class="bg-gray-100 px-1 rounded">addDiscount</code> method instead.</p>
            </div>

          </div>
        </div>
      </section>
    <% end %>
    <% end %>
  </div>
</div>
